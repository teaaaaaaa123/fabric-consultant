---
name: "fabric-consultant"
description: "当用户询问帮我计算耗量、计算耗量、需要多少耗量、耗量多少、用料多少、面料用量、面料耗量时触发。根据版型、尺码、门幅、衣长、裤长、腰围、单排扣、双排扣、全里、半里、无里、三件套、两件套、西装、西裤、大衣、马甲、短裤、腰面、B款、J款、脚口、反撬等条件查询面料计算参考表并计算最终耗量。支持中文与英文提问。Invoke when user asks: 帮我计算耗量, 计算耗量, 需要多少耗量, 耗量多少, 用料多少, 面料用量, 面料耗量, fabric consumption, consumption calculation, how much fabric, fabric usage, fabric requirement, yardage, meterage, material consumption, calculate consumption, how much material."
---

# Fabric Consultant (面料顾问)

## 触发条件

当用户提出以下问题时调用：

1. 版型 + 尺码 + 门幅，问耗量/面料/用料
2. 要求调整衣长、裤长、腰围时重新计算
3. 提到格子面料、条纹面料、对格对条时加量
4. 提到具体版型（如 6KN340、1RZ018、6TF018 等）
5. 提到 B款腰面、J款腰面、反撬等特殊工艺
6. 用英文问以上任意一种（如 "how much fabric for 6TF015 size 50"、"1TF020 size 50 plaid"、"yardage for 6KN340"）

## 语言支持（中文 / English）

### 核心原则

**计算引擎与语言无关。** `query_fabric.py` 的入参只有版型编码和数字（`<版型> <尺码> <门幅> <长度调整> <腰围调整> <格子标记>`），
所以英文提问不改变任何计算逻辑，只需把英文术语翻成上述参数即可。

### 回答语言规则

- 用户用中文问 → 用中文回答。
- 用户用英文问 → **用英文回答**，但以下内容保留中文原文，后面括号补英文：
  - 表名：`TF上衣` (TF Jacket table)、`TF西裤` (TF Trousers table)、`原算料表` (Base table)
  - 版型编码：原样保留（`6TF015`、`1TF020`）
  - 参考表原文行（`matched_item`）：原样保留中文键值，便于用户核对
- 用户中英混用 → 跟随句子主体语言。

### 英文 → 中文术语对照表

**这张表是权威映射，不要临场自行翻译。**

| English | 中文 | 影响 |
|---|---|---|
| plaid / check / checked / windowpane / glen check | 格子 | +10 cm（格子标记=1） |
| solid / plain / plain-colour | 平板 / 素色 | +0 cm（格子标记=0） |
| stripe / striped / pinstripe / chalk stripe | 条纹 | 默认不加量；对条对格需客户明确要求 |
| full lining / fully lined / full lined | 全里 | 匹配「全里」行 |
| half lining / half lined | 半里 | 匹配「半里」行 |
| unlined / no lining | 无里 | 匹配「无里」行 |
| single-breasted / single breasted / SB | 单排扣 | 匹配「单排」行 |
| double-breasted / double breasted / DB | 双排扣 | 匹配「双排」行 |
| flat front / no pleat / plain front | 无褶 | 匹配「无褶」/「正常款」行 |
| single pleat / one pleat | 单褶 | 匹配「单褶」行 |
| double pleat / two pleats | 双褶 | 匹配「双褶」行 |
| jacket / coat(上衣语境) / blazer / suit jacket | 上衣 / 单西 | category=上衣 |
| overcoat / topcoat | 大衣 | category=大衣 |
| trousers / pants / slacks | 西裤 / 裤子 | category=裤子 |
| shorts | 短裤 | 查「短裤」表 |
| waistcoat / vest | 马甲 | category=马甲 |
| two-piece / 2-piece / suit(上衣+裤) | 两件套 | 合计 −20 cm |
| three-piece / 3-piece | 三件套 | 合计 −30 cm |
| width / fabric width / cuttable width | 门幅 | 门幅参数 |
| size | 尺码 | 只接受 40~60 数字码 |
| lengthen / longer / length +N | 衣长/裤长增加 | 耗量 +N×2 |
| shorten / shorter / length −N | 衣长/裤长减少 | 耗量 −N×2 |
| waist +N / let out waist N | 腰围增加 | 耗量 +N |
| turn-up / cuffed hem / TU | 反撬 | 不加裤长耗量 |
| hem opening / leg opening / bottom width | 脚口 | 无收录加量规则，见「已知数据缺口」 |
| B-style waistband / J-style waistband | B款腰面 / J款腰面 | 优先取表中对应项 |
| yard / yardage | 码（1码 = 91.44 cm） | 输出可附码数换算 |
| meter / metre / meterage | 米 | 输出可附米数换算 |

### 英文提问解析示例

| 英文提问 | 解析为 | 命令 |
|---|---|---|
| "how much fabric for 6TF015 size 50?" | 6TF015 / 50 / 门幅74（TF默认） | `json 6TF015 50 74 0 0 0` |
| "1TF020 size 50, plaid fabric" | 1TF020 / 50 / 74 / 格子 | `json 1TF020 50 74 0 0 1` |
| "1TF020 + 6TF015 both size 50" | 两件套，分别查后 −20 | 两次查询后合计 −20 |
| "6KN340 size 48, lengthen 5cm, waist +10" | 长度+5（→+10）、腰围+10 | `json 6KN340 48 74 5 10 0` |
| "yardage for 1TF004 size 48 width 74" | 正常查询 + 码数换算 | `json 1TF004 48 74 0 0 0`，190cm ≈ 2.08 yd |

### 英文提问的注意事项

- **尺码必须是数字码**（40~60）。用户写 `size L` / `medium` / `XL` 时，必须追问对应数字码 —— 表里没有字母码映射关系，不要自行换算。
- 版型编码原样使用，不做大小写以外的改写（脚本内部已 upper 处理）。
- 用户用码/米提问时，内部一律按 cm 计算，最后再换算输出（1 yd = 91.44 cm）。

## 查询步骤

### 第一步：提取关键信息

先从用户问题里提取这些信息，不全请追问确认：

| 信息 | 含义 | 示例 |
|---|---|---|
| 版型编码 | 款号 | 1RZ018、6KN340、6TF018 |
| 尺码 | 40~60码 | 48码、50码 |
| 门幅 | 面料幅宽，单位 cm | 门幅65，门幅74，门幅130，门幅150 |
| 衣长调整 | 衣长 ± cm | 衣长+5，衣长-3 |
| 裤长调整 | 裤长 ± cm | 裤长+10，裤长-2 |
| 腰围调整 | 腰围 ± cm | 腰围+10 |
| 腰面类型 | B款腰面 / J款腰面 | B款腰面 |
| 脚口工艺 | 反撬 / 正常 | 反撬5 |
| 面料风格 | 平板 / 格子 / 条纹 | 格子面料 |

英文提问时，先用上面的「英文 → 中文术语对照表」把词转成这一列信息，再往下走。

**默认规则：**
- 未说明面料风格：默认平板/素色，不加量
- 未说明腰面：默认正常款，不按 J款
- 脚口反撬不加裤长耗量，按工艺说明
- TF系列（6TF开头）如果参考表有多个门幅，用户未指定门幅时默认按**门幅74**输出。
- 1TF系列上衣走独立的 `TF上衣` 表，只有门幅74一档。
- 6TF015~6TF030 中新增的版型只收录了门幅74，没有其他门幅档；问到其他门幅按第四步规则提示。
- 用户一次告诉“一件上衣 + 一件西裤/裤子”时，默认按**两件套/套装组合**计算：各件仍按单件规则分别匹配（上衣匹配“单西”行，裤子匹配裤子行），最后总数 **-20 cm**。
- 用户一次告诉“一件上衣 + 一件西裤/裤子 + 一件马甲”时，默认按**三件套组合**计算：各件仍按单件规则分别匹配，最后总数 **-30 cm**。

### 第二步：读取版型档案

根据版型编码，从 `fabric_reference.json` 中的 `pattern_rules` 读取：

- `category`：上衣/大衣/裤子/马甲
- `规则1`：如戗驳头、平驳头
- `规则2`：尺码范围或衣长/裤长
- `规则3`：单排扣 / 双排扣 / 单褶 / 双褶 / 无褶 / 长裤 / 短裤
- `规则4`：全里 / 半里 / 无里

版型编码规则：
- 1开头 → 上衣
- 4开头 → 大衣
- 6开头 → 裤子
- 5开头 → 马甲

部分版型（如 6TF029、6TF030）没有 `pattern_rules` 档案，但表里有精确行，仍可正常查到耗量；只是无法走「按褶型回落通用行」的路径。

### 第三步：读取面料计算参考表

根据版型类别和规则匹配参考表：

| 类别 | 优先表 | 说明 |
|---|---|---|
| 上衣（1TF开头） | TF上衣 → 原算料表 | 同版型精确匹配优先，门幅只有74 |
| 上衣（其他） | 原算料表 → 全里三件套 → 无里三件套 | 全里/半里/无里要匹配 |
| 大衣 | 大衣 → 猎装 | 含全里/半里/无里 |
| 裤子（非TF系列） | 原算料表 → TF西裤 | 看“单褶/双褶/无褶”匹配 |
| 裤子（6TF开头） | TF西裤 → 原算料表 | 同名版型优先匹配 |
| 马甲 | 原算料表 → 全里三件套 → 无里三件套 | |
| 短裤 | 短裤 | |

匹配时先按门幅筛，再按尺码筛。TF系列（6TF开头）有多个门幅且用户未指定门幅时，默认使用门幅74。

**TF上衣表说明：** 1TF 系列上衣（1TF004/005/006/010/011/012/014/020）有独立的 `TF上衣` 表，平板面料、门幅74、尺码42~60，按版型编码精确匹配，不走原算料表的“单排/双排/全里”通用行。指定其他门幅时按第四步门幅规则处理（无对应档位就提示没有准确耗量）。

**TF西裤表说明：** 6TF015/016/017/019/021/022/023/024/025/026/027/028/029/030 为门幅74、平板、尺码42~60 的独立行。
6TF007/008/009/013/018 的门幅74 数据以**旧值为准**（用户 2026-08-14 明确「原来有的都以之前的为准」），不要用后续截图覆盖。

### 第四步：套用业务规则

拿到基础耗量后，按以下顺序调整：

#### 1. 门幅调整

- 原算料表未单独标注门幅字段的基础耗量，默认按门幅74计算。
- 用户指定门幅不是74时，必须先查参考表/规则是否有该门幅的明确耗量或固定系数。
- 如参考表/规则没有该门幅的明确耗量或固定系数，直接回答“没有这个门幅的准确耗量”，不要自行按比例估算。
- 已知固定规则：
  - 门幅60：基准值 × 1.15
  - 门幅50：基准值 × 1.3225

#### 2. 衣长/裤长调整

- 公式：耗量增加 = 长度增加 × 2
- 例如：裤长+10 → 耗量 +20 cm
- 说明：只加长度部分，其他工艺如反撬不加裤长耗量

#### 3. 腰围调整

- 公式：耗量增加 = 腰围增加
- 例如：腰围+10 → 耗量 +10 cm

#### 4. 面料风格调整

- 格子面料：+10 cm
- 平板/素色：+0 cm
- 条纹/特殊对格对条：按客户具体要求加量

#### 5. 套装/组合默认规则

- 重要：**匹配仍然单独匹配**，套装/三件套只影响最后扣减，不改变匹配项。
- 一次输入包含 1 件上衣 + 1 件西裤/裤子：默认按两件套/套装组合计算；上衣匹配“单西”行，裤子匹配对应裤子行，合计后 **-20 cm**。
- 一次输入包含 1 件上衣 + 1 件西裤/裤子 + 1 件马甲：默认按三件套组合计算；上衣、裤子、马甲分别匹配对应单件行，合计后 **-30 cm**。
- 用户明确说“分开算/单件算/不要套装扣减”时，才不做 -20/-30 扣减。

#### 6. KK系列西裤加量

- 6KK 开头的西裤默认在 KN 耗量表（原算料表 “单襃款西裤”/“双襃款西裤”/“无襃款西裤” 那些行）基础上 **+5cm**。
- 该加量不随尺码/门幅变化，在门幅/裤长/腰围/格子/套装调整之前累加。

#### 7. 特殊版型备注

如果匹配到 J款腰面，参考表中有对应项优先取对应值。

### 第五步：输出结果

把最终计算结果以清晰表格形式给出：

| 项目 | 耗量 (cm) |
|---|---:|
| 基础耗量 | xxx |
| 门幅调整 | +xx |
| 裤长调整 | +xx |
| 腰围调整 | +xx |
| 格子加量 | +xx |
| 套装调整 | -xx |
| **合计** | **xxx** |

然后附上匹配到的参考表原文项。

英文提问时用同结构英文表头（Item / Consumption (cm)），合计后可附 `≈ X.XX m ≈ X.XX yd` 换算。

## 已知数据缺口（不要估算，直接说明）

这些情况参考表和 rules 里都没有依据，必须明确告知用户缺什么，并请他补规则：

| 缺口 | 现状 | 应对 |
|---|---|---|
| 62 / 64 / 66 等大码 | 全部表的尺码列上限为 60 | 回答「超出算料表收录范围」，追问是否有延伸表或每档固定加量 |
| 脚口放大/缩小加量 | `rules` 只有门幅、长度、腰围、格子四条，无脚口规则 | 不自行加量，说明无收录规则并追问 |
| 6KN340 取值歧义 | `pattern_rules` 有档案（水平/男/长裤/无褶），但算料表无 6KN340 行；只能回落原算料表「正常款西裤」，而该名称有两组数据（60码 =193 与 =161） | 说明歧义，请用户指定用哪一组 |

## 数据来源

所有数据来源仅为 `fabric_reference.json`：

- `pattern_rules`：版型档案规则
- `rules`：业务规则（门幅、衣长/裤长、腰围、格子面料）
- `tables`：各算料表原始数据（含 `TF上衣`、`TF西裤`）

不读取真实订单或真实案例数据。

修改 `fabric_reference.json` 前先备份为 `fabric_reference.json.bak-YYYYMMDD-HHMMSS`。

## 注意事项

1. 如果参考表里没有对应门幅的记录，可取最接近门幅档位的值，但要在回答里注明“参考门幅xx档计算”。
2. 如果既没有同版型记录，也没有同规则通用项，回答“该版型未收录到算料表中，请核对版号”。
3. 原算料表默认门幅74；用户指定非74时，只有参考表有对应档或规则有固定系数才计算。没有对应档/固定系数时，必须提示“没有这个门幅的准确耗量”，不要估算。参考表有明确门幅的，直接用表中值。
4. 脚口反撬、腰面 B款/J款，先看参考表里有没有对应工艺项，没有的话按默认款，不作额外加量。
5. 尺码超出 40~60 范围时不要外推，按「已知数据缺口」处理。
6. 英文提问不降低严谨度：缺信息一样追问，缺规则一样明确说明，不要因为语言切换就给估算值。

## OpenClaw 调用说明

### 调用方式

通过命令行调用 Python 脚本，OpenClaw 固定使用 `json` 模式，建议加 `-B` 避免生成缓存文件：

```bash
python -B query_fabric.py json <版型> [尺码] [门幅] [长度调整] [腰围调整] [格子标记]
```

### 参数说明

| 参数位置 | 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 1 | 版型 | string | 是 | 版型编码，如 1KN902、6KN340、6TF018 |
| 2 | 尺码 | string | 否 | 40~60码，如 48、50、54 |
| 3 | 门幅 | float | 否 | 面料幅宽，单位cm，如 65、74、130、150 |
| 4 | 长度调整 | float | 否 | 衣长/裤长调整量，单位cm（正数增加，负数减少） |
| 5 | 腰围调整 | float | 否 | 腰围调整量，单位cm |
| 6 | 格子标记 | int | 否 | 1=格子面料(+10cm)，0=平板面料(不加量)，默认0 |

注意：位置参数不能传空字符串占位（会 `ValueError`）。省略后面的参数就直接不写，例如 `json 1TF012 54`。

### 调用示例

```bash
# 查询6KN340裤子，54码，门幅130
python -B query_fabric.py json 6KN340 54 130 0 0 0

# 查询1KN902上衣，48码，门幅150，衣长+5，腰围+10，格子面料
python -B query_fabric.py json 1KN902 48 150 5 10 1

# 英文提问 "how much fabric for 6TF015 size 50" → TF默认门幅74
python -B query_fabric.py json 6TF015 50 74 0 0 0

# 英文提问 "1TF020 size 50 plaid"
python -B query_fabric.py json 1TF020 50 74 0 0 1

# 只给版型和尺码（门幅走默认）
python -B query_fabric.py json 1TF012 54
```

### 返回结果格式

```json
{
  "ok": true,
  "message": "查询成功",
  "model": "6TF015",
  "size": "50",
  "width": 74,
  "category": "裤子",
  "pattern_rules": {
    "规则1": "男",
    "规则2": "长裤",
    "规则3": "单褶"
  },
  "base_consumption": 135.0,
  "width_adjustment": 0,
  "length_adjustment": 0,
  "waist_adjustment": 0,
  "plaid_adjustment": 0,
  "suit_adjustment": 0,
  "suit_type": null,
  "breakdown": [
    {
      "name": "基础耗量",
      "value": 135.0,
      "description": "参考表基准耗量"
    }
  ],
  "total_consumption": 135.0,
  "matched_item": {
    "版型": "6TF015",
    "面料风格": "平板",
    "门幅": 74,
    "48": 135,
    "50": 135
  },
  "matched_source": "参考表-TF西裤",
  "notes": []
}
```

### 返回字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| ok | bool | 查询是否成功 |
| message | string | 结果消息 |
| model | string | 查询的版型编码 |
| size | string | 查询的尺码 |
| width | float | 查询的门幅 |
| category | string | 版型类别（上衣/大衣/裤子/马甲） |
| pattern_rules | object | 匹配到的版型规则 |
| base_consumption | float | 基础耗量（cm） |
| width_adjustment | float | 门幅调整量（cm） |
| length_adjustment | float | 长度调整量（cm） |
| waist_adjustment | float | 腰围调整量（cm） |
| plaid_adjustment | float | 格子加量（cm） |
| suit_adjustment | float | 套装扣减（cm） |
| suit_type | string | 套装类型（两件套/三件套） |
| breakdown | array | 耗量明细列表 |
| total_consumption | float | 总耗量（cm） |
| matched_item | object | 匹配到的参考表数据项 |
| matched_source | string | 匹配来源表名 |
| notes | array | 备注信息列表 |

返回值的键是英文、值是中文。英文回答时可直译键名，但 `category`、`matched_source`、`matched_item` 的中文值建议保留原文并括号补英文。

### 错误处理

当查询失败时，返回：

```json
{
  "ok": false,
  "message": "该版型未收录到算料表中，请核对版号",
  "model": "XXX",
  ...
}
```

常见 `message`：

| message | 含义 | 英文回答用语 |
|---|---|---|
| 查询成功 | 正常 | Query successful |
| 该版型未收录到算料表中，请核对版号 | 表里无此版型 | Pattern not found in the reference tables — please verify the pattern code |
| 参考表没有门幅XX的准确耗量 | 该门幅无档位也无系数 | No exact consumption data for width XX — cannot estimate |
