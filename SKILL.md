---
name: "fabric-consultant"
description: "当用户询问帮我计算耗量、计算耗量、需要多少耗量、耗量多少、用料多少、面料用量、面料耗量时触发。用于根据版型、尺码、门幅、衣长、裤长、腰围、单排扣、双排扣、全里、半里、无里、三件套、两件套、西装、西裤、大衣、马甲、短裤、腰面、B款、J款、脚口、反撬等条件，查询面料计算参考表，并按规则计算最终耗量。Invoke when user asks: 帮我计算耗量, 计算耗量, 需要多少耗量, 耗量多少, 用料多少, 面料用量, 面料耗量, fabric consumption, consumption calculation."
---

# Fabric Consultant (面料顾问)

## 触发条件

当用户提出以下问题时调用：

1. 版型 + 尺码 + 门幅，问耗量/面料/用料
2. 要求调整衣长、裤长、腰围时重新计算
3. 提到格子面料、条纹面料、对格对条时加量
4. 提到具体版型（如 6KN340、1RZ018、6TF018 等）
5. 提到 B款腰面、J款腰面、反撬等特殊工艺

## 查询步骤

### 第一步：提取关键信息

先从用户问题里提取这些信息，不全请追问确认：

| 信息 | 含义 | 示例 |
|---|---|---|
| 版型编码 | 款号 | 1RZ018、6KN340、6TF018 |
| 尺码 | 40~60码 | 48码、50码 |
| 门幅 | 面料幅宽，单位 cm | 门幅65，门幅74，门幅130，门幅150 |
| 衣长调整 | 衣长 ± cm | 衣长+5，衣长-3 |
| 裤长调整 | 裤长 ± cm | 裤长+10，裤长-2 |
| 腰围调整 | 腰围 ± cm | 腰围+10 |
| 腰面类型 | B款腰面 / J款腰面 | B款腰面 |
| 脚口工艺 | 反撬 / 正常 | 反撬5 |
| 面料风格 | 平板 / 格子 / 条纹 | 格子面料 |

**默认规则：**
- 未说明面料风格：默认平板/素色，不加量
- 未说明腰面：默认正常款，不按 J款
- 脚口反撬不加裤长耗量，按工艺说明

### 第二步：读取版型档案

根据版型编码，从 `fabric_reference.json` 中的 `pattern_rules` 读取：

- `category`：上衣/大衣/裤子/马甲
- `规则1`：如戗驳头、平驳头
- `规则2`：尺码范围或衣长/裤长
- `规则3`：单排扣 / 双排扣 / 单褶 / 双褶 / 无褶 / 长裤 / 短裤
- `规则4`：全里 / 半里 / 无里

版型编码规则：
- 1开头 → 上衣
- 4开头 → 大衣
- 6开头 → 裤子
- 5开头 → 马甲

### 第三步：读取面料计算参考表

根据版型类别和规则匹配参考表：

| 类别 | 优先表 | 说明 |
|---|---|---|
| 上衣 | 原算料表 → 全里三件套 → 无里三件套 | 全里/半里/无里要匹配 |
| 大衣 | 大衣 → 猎装 | 含全里/半里/无里 |
| 裤子（非TF系列） | 原算料表 → TF西裤 | 看“单褶/双褶/无褶”匹配 |
| 裤子（6TF开头） | TF西裤 → 原算料表 | 同名版型优先匹配 |
| 马甲 | 原算料表 → 全里三件套 → 无里三件套 | |
| 短裤 | 短裤 | |

匹配时先按门幅筛，再按尺码筛。

### 第四步：套用业务规则

拿到基础耗量后，按以下顺序调整：

#### 1. 门幅调整

- 门幅60：基准值 × 1.15
- 门幅50：基准值 × 1.3225
- 其他门幅按参考表直接取值，不加系数

#### 2. 衣长/裤长调整

- 公式：耗量增加 = 长度增加 × 2
- 例如：裤长+10 → 耗量 +20 cm
- 说明：只加长度部分，其他工艺如反撬不加裤长耗量

#### 3. 腰围调整

- 公式：耗量增加 = 腰围增加
- 例如：腰围+10 → 耗量 +10 cm

#### 4. 面料风格调整

- 格子面料：+10 cm
- 平板/素色：+0 cm
- 条纹/特殊对格对条：按客户具体要求加量

#### 5. 套装调整

- 两件套：单件基础上减 20 cm
- 三件套：单件基础上减 30 cm

#### 6. 特殊版型备注

如果匹配到 J款腰面，参考表中有对应项优先取对应值。

### 第五步：输出结果

把最终计算结果以清晰表格形式给出：

| 项目 | 耗量 (cm) |
|---|---:|
| 基础耗量 | xxx |
| 门幅调整 | +xx |
| 裤长调整 | +xx |
| 腰围调整 | +xx |
| 格子加量 | +xx |
| 套装调整 | -xx |
| **合计** | **xxx** |

然后附上匹配到的参考表原文项。

## 数据来源

所有数据来源仅为 `fabric_reference.json`：

- `pattern_rules`：版型档案规则
- `rules`：业务规则（门幅、衣长/裤长、腰围、格子面料）
- `tables`：各算料表原始数据

不读取真实订单或真实案例数据。

## 注意事项

1. 如果参考表里没有对应门幅的记录，可取最接近门幅档位的值，但要在回答里注明“参考门幅xx档计算”。
2. 如果既没有同版型记录，也没有同规则通用项，回答“该版型未收录到算料表中，请核对版号”。
3. 门幅计算只在参考表没有对应档时才按规则调整。参考表有明确门幅的，直接用表中值。
4. 脚口反撬、腰面 B款/J款，先看参考表里有没有对应工艺项，没有的话按默认款，不作额外加量。

## OpenClaw 调用说明

### 调用方式

通过命令行调用 Python 脚本，OpenClaw 固定使用 `json` 模式，建议加 `-B` 避免生成缓存文件：

```bash
python -B query_fabric.py json <版型> [尺码] [门幅] [长度调整] [腰围调整] [格子标记]
```

### 参数说明

| 参数位置 | 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 1 | 版型 | string | 是 | 版型编码，如 1KN902、6KN340、6TF018 |
| 2 | 尺码 | string | 否 | 40~60码，如 48、50、54 |
| 3 | 门幅 | float | 否 | 面料幅宽，单位cm，如 65、74、130、150 |
| 4 | 长度调整 | float | 否 | 衣长/裤长调整量，单位cm（正数增加，负数减少） |
| 5 | 腰围调整 | float | 否 | 腰围调整量，单位cm |
| 6 | 格子标记 | int | 否 | 1=格子面料(+10cm)，0=平板面料(不加量)，默认0 |

### 调用示例

```bash
# 查询6KN340裤子，54码，门幅130
python -B query_fabric.py json 6KN340 54 130 0 0 0

# 查询1KN902上衣，48码，门幅150，衣长+5，腰围+10，格子面料
python -B query_fabric.py json 1KN902 48 150 5 10 1
```

### 返回结果格式

```json
{
  "ok": true,
  "message": "查询成功",
  "model": "6KN340",
  "size": "54",
  "width": 130,
  "category": "裤子",
  "pattern_rules": {
    "规则1": "平驳头",
    "规则2": "长裤",
    "规则3": "无褶"
  },
  "base_consumption": 285.5,
  "width_adjustment": 0,
  "length_adjustment": 0,
  "waist_adjustment": 0,
  "plaid_adjustment": 0,
  "breakdown": [
    {
      "name": "基础耗量",
      "value": 285.5,
      "description": "参考表基准耗量"
    }
  ],
  "total_consumption": 285.5,
  "matched_item": {
    "版型": "6KN340 西裤",
    "门幅": 130,
    "48": 265.5,
    "50": 272.3,
    "54": 285.5
  },
  "matched_source": "参考表-TF西裤",
  "notes": []
}
```

### 返回字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| ok | bool | 查询是否成功 |
| message | string | 结果消息 |
| model | string | 查询的版型编码 |
| size | string | 查询的尺码 |
| width | float | 查询的门幅 |
| category | string | 版型类别（上衣/大衣/裤子/马甲） |
| pattern_rules | object | 匹配到的版型规则 |
| base_consumption | float | 基础耗量（cm） |
| width_adjustment | float | 门幅调整量（cm） |
| length_adjustment | float | 长度调整量（cm） |
| waist_adjustment | float | 腰围调整量（cm） |
| plaid_adjustment | float | 格子加量（cm） |
| breakdown | array | 耗量明细列表 |
| total_consumption | float | 总耗量（cm） |
| matched_item | object | 匹配到的参考表数据项 |
| matched_source | string | 匹配来源表名 |
| notes | array | 备注信息列表 |

### 错误处理

当查询失败时，返回：

```json
{
  "ok": false,
  "message": "该版型未收录到算料表中，请核对版号",
  "model": "XXX",
  ...
}
```
